module.exports = {
	trailingComma: 'none', // No trailing commas.
	useTabs: true, // Indent lines with tabs instead of spaces.
	tabWidth: 4, // Specify the number of spaces per indentation-level.
	semi: false, // Do not print semicolons at the ends of statements.
	singleQuote: true, // Use single quotes instead of double quotes.
	jsxSingleQuote: true, // Use single quotes instead of double quotes in JSX.
	bracketSpacing: true, // Print spaces between brackets in object literals.
	jsxBracketSameLine: true, // Put the > of a multi-line JSX element at the end of the last line instead of being alone on the next line
	arrowParens: 'avoid', // Avoid parentheses when possible. 
	endOfLine: 'lf', // Line Feed only (\n), common on Linux and macOS as well as inside git repos
	proseWrap: 'never', // We are using wide monitors(!).
	printWidth: 80,
	arrowParens: 'avoid', // Avoid parentheses around a sole arrow function parameter
	bracketSpacing: true // Checks space need before/after brackets.
}